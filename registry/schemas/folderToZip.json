{
  "operationId": "folderToZip",
  "gatewayPath": "/api/v1/convertors/folder-to-zip",
  "method": "POST",
  "request": {
    "type": "object",
    "additionalProperties": true,
    "oneOf": [
      {
        "type": "object",
        "required": [
          "urls"
        ],
        "properties": {
          "urls": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uri"
            },
            "description": "HTTPS URLs as a JSON array (MCP multipart may send this as a JSON string)"
          },
          "files": {
            "type": "string",
            "format": "binary",
            "description": "Files to pack into a ZIP (repeat the files field, max 50)"
          }
        }
      },
      {
        "type": "object",
        "required": [
          "files"
        ],
        "properties": {
          "files": {
            "type": "string",
            "format": "binary",
            "description": "Files to pack into a ZIP (repeat the files field, max 50)"
          },
          "urls": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uri"
            },
            "description": "HTTPS URLs as a JSON array (MCP multipart may send this as a JSON string)"
          }
        }
      }
    ],
    "description": "Upload a file or pass urls (JSON array of HTTPS URLs)."
  },
  "response": null,
  "responseHint": "Returns ToolFileResponse with downloadUrl"
}