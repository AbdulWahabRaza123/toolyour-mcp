{
  "operationId": "dnsLookupPost",
  "gatewayPath": "/api/v1/security-apis/dns-lookup",
  "method": "POST",
  "request": {
    "oneOf": [
      {
        "type": "object",
        "required": [
          "url"
        ],
        "properties": {
          "url": {
            "type": "string",
            "description": "Domain or URL",
            "example": "example.com"
          },
          "domain": {
            "type": "string",
            "description": "Alias for url"
          }
        }
      },
      {
        "type": "object",
        "required": [
          "inputs"
        ],
        "properties": {
          "inputs": {
            "type": "object",
            "required": [
              "url"
            ],
            "properties": {
              "url": {
                "type": "string",
                "description": "Domain or URL",
                "example": "example.com"
              },
              "domain": {
                "type": "string",
                "description": "Alias for url"
              }
            }
          }
        }
      }
    ],
    "description": "Send fields at the top level, or nested under `inputs`."
  },
  "response": {
    "type": "object",
    "required": [
      "url"
    ],
    "properties": {
      "url": {
        "type": "string",
        "description": "Domain or URL",
        "example": "example.com"
      },
      "domain": {
        "type": "string",
        "description": "Alias for url"
      }
    }
  },
  "responseHint": "Returns JSON response"
}