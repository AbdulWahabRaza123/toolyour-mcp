{
  "operationId": "atsGap",
  "gatewayPath": "/api/v1/text-intelligence/ats-gap",
  "method": "POST",
  "request": {
    "oneOf": [
      {
        "type": "object",
        "required": [
          "jobDescription",
          "resume"
        ],
        "properties": {
          "jobDescription": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100000
          },
          "resume": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100000
          }
        }
      },
      {
        "type": "object",
        "required": [
          "inputs"
        ],
        "properties": {
          "inputs": {
            "type": "object",
            "required": [
              "jobDescription",
              "resume"
            ],
            "properties": {
              "jobDescription": {
                "type": "string",
                "minLength": 1,
                "maxLength": 100000
              },
              "resume": {
                "type": "string",
                "minLength": 1,
                "maxLength": 100000
              }
            }
          }
        }
      }
    ],
    "description": "Send fields at the top level, or nested under `inputs`."
  },
  "response": {
    "type": "object",
    "required": [
      "jobDescription",
      "resume"
    ],
    "properties": {
      "jobDescription": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100000
      },
      "resume": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100000
      }
    }
  },
  "responseHint": "Returns Python API envelope JSON"
}