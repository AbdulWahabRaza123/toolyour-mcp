{
  "operationId": "bcryptHashGenerator",
  "gatewayPath": "/api/v1/security-apis/bcrypt-hash-generator",
  "method": "POST",
  "request": {
    "oneOf": [
      {
        "type": "object",
        "required": [
          "password"
        ],
        "properties": {
          "password": {
            "type": "string",
            "example": "test-only-password"
          },
          "text": {
            "type": "string",
            "description": "Alias for password"
          },
          "algorithm": {
            "type": "string",
            "enum": [
              "bcrypt",
              "argon2id",
              "argon2"
            ],
            "default": "bcrypt"
          },
          "rounds": {
            "type": "integer",
            "minimum": 4,
            "maximum": 14,
            "default": 10
          },
          "cost": {
            "type": "integer",
            "description": "Alias for rounds"
          },
          "iterations": {
            "type": "integer",
            "description": "Argon2 time cost"
          },
          "memorySize": {
            "type": "integer",
            "description": "Argon2 memory (KiB)"
          }
        }
      },
      {
        "type": "object",
        "required": [
          "inputs"
        ],
        "properties": {
          "inputs": {
            "type": "object",
            "required": [
              "password"
            ],
            "properties": {
              "password": {
                "type": "string",
                "example": "test-only-password"
              },
              "text": {
                "type": "string",
                "description": "Alias for password"
              },
              "algorithm": {
                "type": "string",
                "enum": [
                  "bcrypt",
                  "argon2id",
                  "argon2"
                ],
                "default": "bcrypt"
              },
              "rounds": {
                "type": "integer",
                "minimum": 4,
                "maximum": 14,
                "default": 10
              },
              "cost": {
                "type": "integer",
                "description": "Alias for rounds"
              },
              "iterations": {
                "type": "integer",
                "description": "Argon2 time cost"
              },
              "memorySize": {
                "type": "integer",
                "description": "Argon2 memory (KiB)"
              }
            }
          }
        }
      }
    ],
    "description": "Send fields at the top level, or nested under `inputs`."
  },
  "response": {
    "type": "object",
    "required": [
      "password"
    ],
    "properties": {
      "password": {
        "type": "string",
        "example": "test-only-password"
      },
      "text": {
        "type": "string",
        "description": "Alias for password"
      },
      "algorithm": {
        "type": "string",
        "enum": [
          "bcrypt",
          "argon2id",
          "argon2"
        ],
        "default": "bcrypt"
      },
      "rounds": {
        "type": "integer",
        "minimum": 4,
        "maximum": 14,
        "default": 10
      },
      "cost": {
        "type": "integer",
        "description": "Alias for rounds"
      },
      "iterations": {
        "type": "integer",
        "description": "Argon2 time cost"
      },
      "memorySize": {
        "type": "integer",
        "description": "Argon2 memory (KiB)"
      }
    }
  },
  "responseHint": "Returns JSON response"
}