{
  "operationId": "zipExtract",
  "gatewayPath": "/api/v1/convertors/zip-extract",
  "method": "POST",
  "request": {
    "type": "object",
    "additionalProperties": true,
    "oneOf": [
      {
        "type": "object",
        "required": [
          "urls"
        ],
        "properties": {
          "urls": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uri"
            },
            "description": "HTTPS URLs as a JSON array (MCP multipart may send this as a JSON string)"
          },
          "file": {
            "type": "string",
            "format": "binary",
            "description": "ZIP file to extract, or pass a ZIP URL in urls"
          }
        }
      },
      {
        "type": "object",
        "required": [
          "file"
        ],
        "properties": {
          "file": {
            "type": "string",
            "format": "binary",
            "description": "ZIP file to extract, or pass a ZIP URL in urls"
          },
          "urls": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uri"
            },
            "description": "HTTPS URLs as a JSON array (MCP multipart may send this as a JSON string)"
          }
        }
      }
    ],
    "description": "Upload a file or pass urls (JSON array of HTTPS URLs)."
  },
  "response": null,
  "responseHint": "Returns Node API envelope JSON"
}