{
  "operationId": "atsGapFile",
  "gatewayPath": "/api/v1/text-intelligence/ats-gap-file",
  "method": "POST",
  "request": {
    "type": "object",
    "required": [
      "jobDescription",
      "resume"
    ],
    "properties": {
      "jobDescription": {
        "type": "string",
        "description": "Job posting text to compare against the resume"
      },
      "resume": {
        "type": "string",
        "format": "binary",
        "description": "Resume file (PDF, DOCX, or TXT — max 10 MB)"
      }
    }
  },
  "response": null,
  "responseHint": "Returns Python API envelope JSON"
}