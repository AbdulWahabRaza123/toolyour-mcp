{
  "operationId": "aiFileInsights",
  "gatewayPath": "/api/v1/ai-models/file-insights",
  "method": "POST",
  "request": {
    "type": "object",
    "required": [
      "file",
      "prompt"
    ],
    "properties": {
      "file": {
        "type": "string",
        "format": "binary",
        "description": "Document or image (PDF, DOCX, TXT, CSV, or common image types — max 10 MB)"
      },
      "prompt": {
        "type": "string",
        "description": "Instructions for the AI analysis",
        "example": "Summarize the key points and list action items."
      },
      "geminiApiKey": {
        "type": "string",
        "description": "Optional. Your Google Gemini API key (BYOK). When set, ToolYour does not use platform Gemini keys and does not charge ToolYour credits for the Gemini call. Prefer also sending header X-Gemini-Api-Key (works before multipart parse). Never logged or stored."
      }
    }
  },
  "response": null,
  "responseHint": "Returns Node API envelope JSON"
}