{
  "operationId": "aiFileInsights",
  "gatewayPath": "/api/v1/ai-models/file-insights",
  "method": "POST",
  "request": {
    "type": "object",
    "required": [
      "file",
      "prompt",
      "geminiApiKey"
    ],
    "properties": {
      "file": {
        "type": "string",
        "format": "binary",
        "description": "Document, image, audio, or video (PDF, DOC, DOCX, TXT, CSV, TSV, XLS, XLSX, common images, audio, video — max 10 MB)"
      },
      "prompt": {
        "type": "string",
        "description": "Instructions for the AI analysis",
        "example": "Summarize the key points and list action items."
      },
      "geminiApiKey": {
        "type": "string",
        "description": "Required. Your Google Gemini API key (BYOK). Prefer header X-Gemini-Api-Key (works before multipart parse). Never logged or stored. ToolYour credits are not charged for the Gemini call."
      }
    }
  },
  "response": null,
  "responseHint": "Returns Node API envelope JSON"
}