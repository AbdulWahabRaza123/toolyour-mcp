{
  "operationId": "convertToGrayscale",
  "gatewayPath": "/api/v1/convertors/to-grayscale",
  "method": "POST",
  "request": {
    "type": "object",
    "additionalProperties": true,
    "oneOf": [
      {
        "type": "object",
        "required": [
          "urls"
        ],
        "properties": {
          "urls": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uri"
            },
            "description": "HTTPS URLs as a JSON array (MCP multipart may send this as a JSON string)"
          },
          "image": {
            "type": "string",
            "format": "binary",
            "description": "Image, ZIP, or RAR to convert to grayscale"
          }
        }
      },
      {
        "type": "object",
        "required": [
          "image"
        ],
        "properties": {
          "image": {
            "type": "string",
            "format": "binary",
            "description": "Image, ZIP, or RAR to convert to grayscale"
          },
          "urls": {
            "type": "array",
            "items": {
              "type": "string",
              "format": "uri"
            },
            "description": "HTTPS URLs as a JSON array (MCP multipart may send this as a JSON string)"
          }
        }
      }
    ],
    "description": "Upload a file or pass urls (JSON array of HTTPS URLs)."
  },
  "response": null,
  "responseHint": "Returns ToolFileResponse with downloadUrl"
}