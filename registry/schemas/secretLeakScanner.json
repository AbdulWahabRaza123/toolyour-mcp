{
  "operationId": "secretLeakScanner",
  "gatewayPath": "/api/v1/security-apis/secret-leak-scanner",
  "method": "POST",
  "request": {
    "oneOf": [
      {
        "type": "object",
        "required": [
          "text"
        ],
        "properties": {
          "text": {
            "type": "string",
            "description": "Pastes of env/config/logs to scan for secret patterns",
            "example": "AWS_ACCESS_KEY_ID=AKIA..."
          },
          "content": {
            "type": "string",
            "description": "Alias for text"
          },
          "input": {
            "type": "string",
            "description": "Alias for text"
          }
        }
      },
      {
        "type": "object",
        "required": [
          "inputs"
        ],
        "properties": {
          "inputs": {
            "type": "object",
            "required": [
              "text"
            ],
            "properties": {
              "text": {
                "type": "string",
                "description": "Pastes of env/config/logs to scan for secret patterns",
                "example": "AWS_ACCESS_KEY_ID=AKIA..."
              },
              "content": {
                "type": "string",
                "description": "Alias for text"
              },
              "input": {
                "type": "string",
                "description": "Alias for text"
              }
            }
          }
        }
      }
    ],
    "description": "Send fields at the top level, or nested under `inputs`."
  },
  "response": {
    "type": "object",
    "required": [
      "text"
    ],
    "properties": {
      "text": {
        "type": "string",
        "description": "Pastes of env/config/logs to scan for secret patterns",
        "example": "AWS_ACCESS_KEY_ID=AKIA..."
      },
      "content": {
        "type": "string",
        "description": "Alias for text"
      },
      "input": {
        "type": "string",
        "description": "Alias for text"
      }
    }
  },
  "responseHint": "Returns JSON response"
}