{
  "operationId": "spfDkimDmarcCheckerPost",
  "gatewayPath": "/api/v1/security-apis/spf-dkim-dmarc-checker",
  "method": "POST",
  "request": {
    "oneOf": [
      {
        "type": "object",
        "required": [
          "url"
        ],
        "properties": {
          "url": {
            "type": "string",
            "description": "Domain or URL (hostname extracted when a URL is passed)",
            "example": "example.com"
          }
        }
      },
      {
        "type": "object",
        "required": [
          "inputs"
        ],
        "properties": {
          "inputs": {
            "type": "object",
            "required": [
              "url"
            ],
            "properties": {
              "url": {
                "type": "string",
                "description": "Domain or URL (hostname extracted when a URL is passed)",
                "example": "example.com"
              }
            }
          }
        }
      }
    ],
    "description": "Send fields at the top level, or nested under `inputs`."
  },
  "response": {
    "type": "object",
    "required": [
      "url"
    ],
    "properties": {
      "url": {
        "type": "string",
        "description": "Domain or URL (hostname extracted when a URL is passed)",
        "example": "example.com"
      }
    }
  },
  "responseHint": "Returns JSON response"
}