{
  "operationId": "sslTlsCertificateCheckerPost",
  "gatewayPath": "/api/v1/security-apis/ssl-tls-certificate-checker",
  "method": "POST",
  "request": {
    "oneOf": [
      {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "format": "uri",
            "example": "https://example.com"
          },
          "pem": {
            "type": "string",
            "description": "Offline certificate PEM"
          },
          "certificate": {
            "type": "string",
            "description": "Alias for pem"
          },
          "csr": {
            "type": "string",
            "description": "Certificate signing request PEM"
          }
        }
      },
      {
        "type": "object",
        "required": [
          "inputs"
        ],
        "properties": {
          "inputs": {
            "type": "object",
            "properties": {
              "url": {
                "type": "string",
                "format": "uri",
                "example": "https://example.com"
              },
              "pem": {
                "type": "string",
                "description": "Offline certificate PEM"
              },
              "certificate": {
                "type": "string",
                "description": "Alias for pem"
              },
              "csr": {
                "type": "string",
                "description": "Certificate signing request PEM"
              }
            }
          }
        }
      }
    ],
    "description": "Send fields at the top level, or nested under `inputs`."
  },
  "response": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "format": "uri",
        "example": "https://example.com"
      },
      "pem": {
        "type": "string",
        "description": "Offline certificate PEM"
      },
      "certificate": {
        "type": "string",
        "description": "Alias for pem"
      },
      "csr": {
        "type": "string",
        "description": "Certificate signing request PEM"
      }
    }
  },
  "responseHint": "Returns JSON response"
}