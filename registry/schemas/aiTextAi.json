{
  "operationId": "aiTextAi",
  "gatewayPath": "/api/v1/ai-models/text-ai",
  "method": "POST",
  "request": {
    "oneOf": [
      {
        "type": "object",
        "required": [
          "prompt"
        ],
        "properties": {
          "prompt": {
            "type": "string",
            "example": "Summarize this topic in 3 bullet points."
          },
          "geminiApiKey": {
            "type": "string",
            "description": "Optional. Your Google Gemini API key (BYOK). When set, ToolYour does not use platform Gemini keys and does not charge ToolYour credits for the Gemini call. Prefer also sending header X-Gemini-Api-Key. Never logged or stored."
          }
        }
      },
      {
        "type": "object",
        "required": [
          "inputs"
        ],
        "properties": {
          "inputs": {
            "type": "object",
            "required": [
              "prompt"
            ],
            "properties": {
              "prompt": {
                "type": "string",
                "example": "Summarize this topic in 3 bullet points."
              },
              "geminiApiKey": {
                "type": "string",
                "description": "Optional. Your Google Gemini API key (BYOK). When set, ToolYour does not use platform Gemini keys and does not charge ToolYour credits for the Gemini call. Prefer also sending header X-Gemini-Api-Key. Never logged or stored."
              }
            }
          }
        }
      }
    ],
    "description": "Send fields at the top level, or nested under `inputs`."
  },
  "response": {
    "type": "object",
    "required": [
      "prompt"
    ],
    "properties": {
      "prompt": {
        "type": "string",
        "example": "Summarize this topic in 3 bullet points."
      },
      "geminiApiKey": {
        "type": "string",
        "description": "Optional. Your Google Gemini API key (BYOK). When set, ToolYour does not use platform Gemini keys and does not charge ToolYour credits for the Gemini call. Prefer also sending header X-Gemini-Api-Key. Never logged or stored."
      }
    }
  },
  "responseHint": "Returns Node API envelope JSON"
}