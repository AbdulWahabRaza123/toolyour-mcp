{
  "operationId": "aiTextAi",
  "gatewayPath": "/api/v1/ai-models/text-ai",
  "method": "POST",
  "request": {
    "oneOf": [
      {
        "type": "object",
        "required": [
          "prompt",
          "geminiApiKey"
        ],
        "properties": {
          "prompt": {
            "type": "string",
            "example": "Summarize this topic in 3 bullet points."
          },
          "geminiApiKey": {
            "type": "string",
            "description": "Required. Your Google Gemini API key (BYOK). ToolYour does not use a platform Gemini key. Prefer also sending header X-Gemini-Api-Key. Never logged or stored. ToolYour credits are not charged for the Gemini call."
          }
        }
      },
      {
        "type": "object",
        "required": [
          "inputs"
        ],
        "properties": {
          "inputs": {
            "type": "object",
            "required": [
              "prompt",
              "geminiApiKey"
            ],
            "properties": {
              "prompt": {
                "type": "string",
                "example": "Summarize this topic in 3 bullet points."
              },
              "geminiApiKey": {
                "type": "string",
                "description": "Required. Your Google Gemini API key (BYOK). ToolYour does not use a platform Gemini key. Prefer also sending header X-Gemini-Api-Key. Never logged or stored. ToolYour credits are not charged for the Gemini call."
              }
            }
          }
        }
      }
    ],
    "description": "Send fields at the top level, or nested under `inputs`."
  },
  "response": {
    "type": "object",
    "required": [
      "prompt",
      "geminiApiKey"
    ],
    "properties": {
      "prompt": {
        "type": "string",
        "example": "Summarize this topic in 3 bullet points."
      },
      "geminiApiKey": {
        "type": "string",
        "description": "Required. Your Google Gemini API key (BYOK). ToolYour does not use a platform Gemini key. Prefer also sending header X-Gemini-Api-Key. Never logged or stored. ToolYour credits are not charged for the Gemini call."
      }
    }
  },
  "responseHint": "Returns Node API envelope JSON"
}